// No need for imports for basic setup
// The defineSetupVue3 export is not available in the current version

// Import components
import Callout from '../components/Callout.vue'
import Button from '../components/Button.vue'

// Setup function for the theme
export default () => {
  // Return the components to be globally registered
  return {
    // No need to register components here
    // Slidev will handle component registration differently
  }
} 